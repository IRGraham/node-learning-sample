'use strict';
/**
 * Use Strict ESLint rules
 * For detailed rule explanations, see http://eslint.org/docs/rules/#strict
 */
module.exports = {
    'rules': {
        // controls location of Use Strict Directives -- we use a plugin which already adds use strict to our files
        'strict': [2, 'global']
    }
};
